{
  "name": "ra-interface-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for the OpenBao PKI RA Console UI",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageReporters": ["text", "lcov"],
    "collectCoverageFrom": ["utils.js"]
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
